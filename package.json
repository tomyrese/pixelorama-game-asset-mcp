{
  "name": "pixelorama-game-asset-mcp-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "build": "pnpm -r --filter=!pixelorama-extension build",
    "test": "vitest run",
    "typecheck": "pnpm -r --filter=!pixelorama-extension typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "ws": "^8.21.3"
  }
}
